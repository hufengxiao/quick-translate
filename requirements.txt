# Quick Translate dependencies
# Core: Python 3.10+ with tkinter (no external deps for basic functionality)

# Recommended:
loguru>=0.7.0              # Structured logging (falls back to stdlib if missing)

# Optional:
pywin32>=306               # Windows COM — enables TTS pronunciation (SAPI5)
pyperclip>=1.8.2           # Clipboard access (fallback if ctypes fails)

# Build:
# pyinstaller>=6.0.0       # For building single-file EXE
# upx                      # For EXE compression (optional, download separately)
